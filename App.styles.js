// Import necessary modules from React Native
import { StyleSheet } from 'react-native';

// Define your global variables
const globalVars = {
    boldColor: '#8d1a4c',
    lightColor: 'rgb(189, 0, 82)',
    shadowColor: '#a50047e1',
    lessShadowColor: '#a5004781',
    decorColor: 'rgb(29, 181, 192)',
    decorColorShadow: 'rgba(35, 179, 189, 0.09)',
    boldGray: 'rgb(78, 78, 78)',
    lightGray: 'rgba(95, 95, 95, 0.966)',
    shadowGray: 'rgba(95, 95, 95, 0.699)',
    fontSizeTopic: 48, // Update with the desired values
    fontSizeSemitopic: 22, // Update with the desired values
    fontSizeNormal: 16, // Update with the desired values
    fontSizeP: 14, // Update with the desired values
    fontSizeSmall: 12, // Update with the desired values
    fontFamily: 'RethinkSans-Regular',
};

// Define your global styles
const globalStyles = StyleSheet.create({
});

// Export the global variables and styles
export { globalVars, globalStyles };
